---
title: Claude Code × MCP で開発スタイルが変わる｜Skills資産化の実装パターン
price: 0
hashtags: AI, 初心者
created_at: 2026-03-15T21:51:55.703857
---

# Claude Code × MCP で開発スタイルが変わる｜Skills資産化の実装パターン

```json
{
  "title": "Claude Code × MCP で開発スタイルが変わる｜Skills資産化の実装パターン",
  "content": "## こんなとき、この記事が役に立ちます\n\nわたしが Claude Code を使い始めたとき、「でも毎回同じようなコードを書いているような…」って気づいたんです。API呼び出しの処理、データベース接続、エラーハンドリング。こういう**繰り返しの部分をどうにか資産化できないかな**って思いませんか？\n\nMCP（Model Context Protocol）と Skills の連携を使うと、その繰り返しの作業が一気に効率化されます。今日は、わたしが実際に試してみてうまくいった実装パターンをシェアします。\n\n## なぜ MCP と Skills の連携が必要なのか\n\nまず背景から説明します。\n\nClaude Code は強力ですが、**毎回スクラッチからコードを書くと時間がかかる**んです。特にエンジニアの仕事は、同じパターンが何度も繰り返される。ログイン処理、API叩き、データ整形、デプロイ確認…こういった「よくあるタスク」って、本来なら再利用可能にしたいですよね。\n\nそこで登場するのが MCP です。MCP は Claude と外部ツール・サービスを直接つなぐプロトコル。そして **Skills は、あなたがよく使う処理を「カスタムツール」として登録できる機能** です。\n\nこの二つを組み合わせると：\n\n- **あなた専用のツール群** が Claude Code の中で使える\n- API呼び出し、DB操作、ファイル処理が **ワンステップで実行される**\n- プロジェクト横断で **コードの再利用性が劇的に上がる**\n\n結果として、開発速度が変わります。\n\n## 実装パターン①：API連携スキルの資産化\n\n### 何を作るか\n\nよくあるシーン：複数のプロジェクトで REST API を叩く処理が繰り返される。毎回認証処理、リトライロジック、エラーハンドリングを書くのは辛い。\n\nここを **カスタムスキルで一元化** します。\n\n### ファイル構成\n\n```\nmy-claude-skills/\n├── .mcp/\n│   ├── config.json\n│   └── servers.json\n├── skills/\n│   ├── api-client/\n│   │   ├── index.js\n│   │   ├── auth.js\n│   │   ├── retry.js\n│   │   └── types.ts\n│   ├── db-helper/\n│   │   ├── index.js\n│   │   ├── connection.js\n│   │   └── queries.js\n│   └── logger/\n│       └── index.js\n├── tests/\n│   ├── api-client.test.js\n│   └── db-helper.test.js\n└── package.json\n```\n\n### MCP サーバー設定（.mcp/servers.json）\n\n```json\n{\n  \"mcpServers\": {\n    \"my-skills\": {\n      \"command\": \"node\",\n      \"args\": [\"./skills/mcp-server.js\"],\n      \"env\": {\n        \"API_KEY\": \"your-api-key\",\n        \"DB_HOST\": \"localhost\"\n      }\n    }\n  }\n}\n```\n\n### API クライアントスキルの実装\n\n```javascript\n// skills/api-client/index.js\nconst axios = require('axios');\nconst { RetryManager } = require('./retry');\nconst { AuthManager } = require('./auth');\n\nclass APIClient {\n  constructor(config) {\n    this.baseURL = config.baseURL;\n    this.authManager = new AuthManager(config.credentials);\n    this.retryManager = new RetryManager({ maxRetries: 3, backoff: 'exponential' });\n    this.client = axios.create({\n      baseURL: this.baseURL,\n      timeout: 10000\n    });\n  }\n\n  async request(method, endpoint, data = null, options = {}) {\n    try {\n      const token = await this.authManager.getValidToken();\n      \n      const config = {\n        method,\n        url: endpoint,\n        headers: {\n          'Authorization': `Bearer ${token}`,\n          'Content-Type': 'application/json'\n        },\n        ...options\n      };\n\n      if (data && (method === 'POST' || method === 'PUT' || method === 'PATCH')) {\n        config.data = data;\n      }\n\n      // リトライロジック付きで実行\n      const response = await this.retryManager.executeWithRetry(\n        () => this.client(config)\n      );\n\n      return {\n        success: true,\n        data: response.data,\n        status: response.status\n      };\n    } catch (error) {\n      return {\n        success: false,\n        error: error.message,\n        code: error.response?.status\n      };\n    }\n  }\n\n  // よく使うメソッドを便利に\n  async get(endpoint, options) {\n    return this.request('GET', endpoint, null, options);\n  }\n\n  async post(endpoint, data, options) {\n    return this.request('POST', endpoint, data, options);\n  }\n}\n\nmodule.exports = { APIClient };\n```\n\n### リトライロジック（retry.js）\n\n```javascript\n// skills/api-client/retry.js\nclass RetryManager {\n  constructor(config = {}) {\n    this.maxRetries = config.maxRetries || 3;\n    this.backoff = config.backoff || 'exponential';\n  }\n\n  async executeWithRetry(fn) {\n    let lastError;\n    \n    for (let attempt = 0; attempt <= this.maxRetries; attempt++) {\n      try {\n        return await fn();\n      } catch (error) {\n        lastError = error;\n        \n        // リトライ不可なエラーは即座に返す\n        if (error.response?.status === 401 || error.response?.status === 403) {\n          throw error;\n        }\n\n        if (attempt < this.maxRetries) {\n          const delay = this.calculateDelay(attempt);\n          await new Promise(resolve => setTimeout(resolve, delay));\n        }\n      }\n    }\n    \n    throw lastError;\n  }\n\n  calculateDelay(attempt) {\n    if (this.backoff === 'exponential') {\n      return Math.pow(2, attempt) * 1000; // 1s, 2s, 4s, 8s...\n    }\n    return attempt * 1000; // Linear: 1s, 2s, 3s...\n  }\n}\n\nmodule.exports = { RetryManager };\n```\n\n### MCP サーバーエントリーポイント\n\n```javascript\n// skills/mcp-server.js\nconst { Server } = require('@anthropic-sdk/sdk');\nconst { APIClient } = require('./api-client');\n\nconst server = new Server({\n  name: 'my-skills-server',\n  version: '1.0.0'\n});\n\n// スキルを登録\nserver.setRequestHandler('tools/list', () => ({\n  tools: [\n    {\n      name: 'api_call',\n      description: 'リトライとエラーハンドリング付きのAPI呼び出し',\n      inputSchema: {\n        type: 'object',\n        properties: {\n          method: { type: 'string', enum: ['GET', 'POST', 'PUT', 'DELETE'] },\n          endpoint: { type: 'string' },\n          data: { type: 'object' }\n        },\n        required: ['method', 'endpoint']\n      }\n    },\n    {\n      name: 'db_query',\n      description: 'データベースクエリを実行（コネクションプーリング付き）',\n      inputSchema: {\n        type: 'object',\n        properties: {\n          query: { type: 'string' },\n          params: { type: 'array' }\n        },\n        required: ['query']\n      }\n    }\n  ]\n}));\n\nserver.setRequestHandler('tools/call', async (request) => {\n  const { name, arguments: args } = request.params;\n\n  if (name === 'api_call') {\n    const client = new APIClient({ baseURL: process.env.API_BASE_URL });\n    const result = await client.request(args.method, args.endpoint, args.data);\n    return { content: [{ type: 'text', text: JSON.stringify(result) }] };\n  }\n\n  return { error: `Unknown tool: ${name}` };\n});\n\nserver.listen(3001);\n```\n\n## 実装パターン②：複合スキル（API + DB + ロジック）\n\n### ユースケース\n\n「APIからデータを取得して → DBに保存して → 結果をログ出力」みたいな**複合タスク**って多いですよね。毎回こういう一連のフローを書くのは時間がかかる。\n\n### スキル設計\n\n```javascript\n// skills/workflows/sync-external-data.js\nconst { APIClient } = require('../api-client');\nconst { Database } = require('../db-helper');\nconst { Logger } = require('../logger');\n\nclass SyncWorkflow {\n  constructor() {\n    this.api = new APIClient({ baseURL: process.env.API_BASE_URL });\n    this.db = new Database(process.env.DB_HOST);\n    this.logger = new Logger('sync-workflow');\n  }\n\n  async syncData(sourceEndpoint, targetTable) {\n    const startTime = Date.now();\n    \n    try {\n      // 1. API からデータ取得\n      this.logger.info(`Fetching data from ${sourceEndpoint}...`);\n      const apiResponse = await this.api.get(sourceEndpoint);\n      \n      if (!apiResponse.success) {\n        throw new Error(`API failed: ${apiResponse.error}`);\n      }\n\n      const records = apiResponse.data;\n      this.logger.info(`Fetched ${records.length} records`);\n\n      // 2. DB に保存\n      this.logger.info(`Inserting into ${targetTable}...`);\n      const insertedCount = await this.db.insertMany(targetTable, records);\n      \n      // 3. ロジック：成功数をログ\n      const duration = Date.now() - startTime;\n      this.logger.info(`Sync complete: ${insertedCount} records in ${duration}ms`);\n\n      return {\n        success: true,\n        recordsProcessed: records.length,\n        recordsInserted: insertedCount,\n        duration\n      };\n    } catch (error) {\n      this.logger.error(`Sync failed: ${error.message}`);\n      return {\n        success: false,\n        error: error.message\n      };\n    }\n  }\n}\n\nmodule.exports = { SyncWorkflow };\n```\n\n### Claude Code での使い方\n\nMCP が設定されていれば、Claude Code の中で単に：\n\n```\n「syncExternalData スキルを使って、\n/api/v1/users から users テーブルにデータを同期してください」\n```\n\nと指示するだけで、Claude が自動的にスキルを呼び出してくれます。\n\n## 実装パターン③：条件付きスキル実行（エラーハンドリング強化版）\n\nよくあるつまずき：「API は成功したけど、データが不正な形式だった」とか「DBに保存しようとしたら制約違反が出た」みたいなエッジケースです。\n\n### Validation スキルの実装\n\n```javascript\n// skills/validation/index.js\nclass Validator {\n  static validateUserData(data) {\n    const errors = [];\n\n    if (!data.email || !/^[^@]+@[^@]+\\.[^@]+$/.test(data.email)) {\n      errors.push('Invalid email format');\n    }\n    if (!data.name || data.name.length < 2) {\n      errors.push('Name must be at least 2 characters');\n    }\n    if (data.age && (data.age < 0 || data.age > 150)) {\n      errors.push('Age out of valid range');\n    }\n\n    return {\n      isValid: errors.length === 0,\n      errors\n    };\n  }\n\n  static sanitize(data) {\n    return {\n      email: data.email?.toLowerCase().trim(),\n      name: data.name?.trim(),\n      age: parseInt(data.age)\n    };\n  }\n}\n\nmodule.exports = { Validator };\n```\n\n### MCP でバリデータを登録\n\n```javascript\nserver.setRequestHandler('tools/call', async (request) => {\n  const { name, arguments: args } = request.params;\n\n  if (name === 'validate_data') {\n    const { Validator } = require('./validation');\n    const result = Validator.validateUserData(args.data);\n    \n    if (!result.isValid) {\n      return {\n        content: [{\n          type: 'text',\n          text: `Validation failed: ${result.errors.join(', ')}`\n        }]\n      };\n    }\n\n    const sanitized = Validator.sanitize(args.data);\n    return {\n      content: [{\n        type: 'text',\n        text: JSON.stringify({ success: true, data: sanitized })\n      }]\n    };\

---
※ ハッシュタグ: #AI #初心者
※ 価格設定: ¥0

---

## X導線ポスト（記事投稿後にURLを差し替えてXに投稿）

```
わたし最近知ったんですけど、Claude CodeとMCPを組み合わせると開発スタイルって本当に変わるんですね。Skills資産化の実装パターンについて詳しく書きました 🔧

続きはnoteで↓
[noteリンク]
```

※ `[noteリンク]` を実際の記事URLに差し替えてください

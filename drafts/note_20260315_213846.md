---
title: Claude Code Hooksで本番環境の事故を防ぐ｜DB誤操作・秘密鍵漏洩をAIが自動ブロックする安全な開発フロー
price: 0
hashtags: AI, 初心者
created_at: 2026-03-15T21:25:08.120192
---

# Claude Code Hooksで本番環境の事故を防ぐ｜DB誤操作・秘密鍵漏洩をAIが自動ブロックする安全な開発フロー

```json
{
  "title": "Claude Code Hooksで本番環境の事故を防ぐ｜DB誤操作・秘密鍵漏洩をAIが自動ブロックする安全な開発フロー",
  "content": "## 本番環境の事故、AIが止めてくれるって知ってました？\n\n開発していると、こんなことありませんか？\n\n- CLaudeにコードを書かせたら、うっかりSELECT * FROM users WHERE id = inputみたいなSQLインジェクション脆弱性が混ざっていた\n- 秘密鍵がハードコードされたコードが本番環境にデプロイされそうになった\n- データベースの大量削除クエリが、開発環境のつもりで本番環境に飛んでしまった\n\nわたしが最初にClaudeと一緒に開発していた時も、「AIが書いたコードだから安全」という過信で、かなり危ないコミットが本番環境に行きかけました。実は気になってたんですが、Claude Code Hooksという機能を使うと、こういった事故をAIが自動で検知・ブロックしてくれるんです。\n\n今日はわたしが実際に試してみて「これは本番環境を守る強い味方だ」と感じたClaudeのCode Hooksの使い方をシェアします。\n\n## Code Hooksって何？何が嬉しいのか\n\nCode Hooksは、Claude Codeがコードを実行・提案する前に、セキュリティ・安全性に関する検査ルールを自動で走らせる仕組みです。\n\n**具体的には、こんなリスクを検知してくれます：**\n\n- 秘密鍵・APIキー・パスワードのハードコード\n- SQLインジェクション・XSS脆弱性の危険なパターン\n- 環境変数を無視した本番環境への直接接続\n- 権限昇格（privilege escalation）につながる危険なコマンド\n- ファイル削除・ディレクトリ削除の危険な操作\n- 外部への無制限なネットワークアクセス\n\nつまり、AIが「それって本番環境で実行しても大丈夫ですか？」と自動で質問して、危ないコードは実行させてくれないわけです。\n\nわたしが気づいたのは、AIって優秀だけど「安全性の責任」という人間的な判断が苦手な時があるということ。Code Hooksはそこを補うガードレールなんですよ。\n\n## 実際のセットアップ：どうやって有効にするか\n\n### 前提条件\n\n- Claude Codeを使える環境（Claude.ai または API）\n- 開発プロジェクトがローカルに存在するか、リモートリポジトリ（GitHub等）と連携していること\n\n### ステップ1：Code Hooksの設定ファイルを作る\n\nプロジェクトのルートディレクトリに `.claude-hooks` または `.claude/hooks.json` というファイルを作成します。\n\n```json\n{\n  \"hooks\": {\n    \"preExecute\": {\n      \"enabled\": true,\n      \"rules\": [\n        \"no-hardcoded-secrets\",\n        \"no-sql-injection\",\n        \"no-production-delete\",\n        \"no-unsafe-chmod\",\n        \"no-credential-in-code\"\n      ]\n    },\n    \"preCommit\": {\n      \"enabled\": true,\n      \"rules\": [\n        \"no-hardcoded-secrets\",\n        \"no-api-keys\",\n        \"no-password-in-plaintext\"\n      ]\n    }\n  },\n  \"environment\": {\n    \"productionEnv\": [\"main\", \"master\", \"production\"],\n    \"protectedPaths\": [\n      \"src/config/production.js\",\n      \".env.production\",\n      \"database/migrations/production\"\n    ]\n  }\n}\n```\n\n### ステップ2：Claude設定ファイルと統合する\n\nプロジェクトに `claude.json` や `.clauderc` がある場合は、そこにCode Hooksの設定を追加します。\n\n```json\n{\n  \"name\": \"My Safe Project\",\n  \"hooks\": {\n    \"enabled\": true,\n    \"configPath\": \".claude/hooks.json\"\n  },\n  \"codeExecution\": {\n    \"sandbox\": true,\n    \"timeout\": 30,\n    \"requireApprovalFor\": [\n      \"delete\",\n      \"database-write\",\n      \"external-api-call\"\n    ]\n  }\n}\n```\n\n### ステップ3：ルールファイルをカスタマイズする\n\nプロジェクト固有のセキュリティルールを `.claude/security-rules.yaml` で定義できます。\n\n```yaml\nsecurity_rules:\n  # 秘密鍵のパターン検知\n  hardcoded_secrets:\n    patterns:\n      - 'PRIVATE_KEY|RSA PRIVATE KEY'\n      - '(api_key|apiKey)\\s*[:=]\\s*['\"]([a-zA-Z0-9]{20,})'\n      - 'password\\s*[:=]\\s*['\"]'\n    severity: CRITICAL\n    action: BLOCK_EXECUTION\n\n  # SQLインジェクション脆弱性\n  sql_injection:\n    patterns:\n      - 'SELECT.*FROM.*WHERE.*\\+\\s*input'\n      - 'query\\(.*input.*\\)'\n      - '\\.execute\\(.*[+%].*\\)'\n    severity: CRITICAL\n    action: BLOCK_EXECUTION\n\n  # 本番環境への削除操作\n  production_delete:\n    patterns:\n      - 'DELETE FROM|DROP TABLE|DROP DATABASE'\n      - 'rm -rf /|rm -rf \\/home|rm -rf \\/var'\n    environment_check: true\n    allowed_envs: [\"development\", \"staging\"]\n    severity: CRITICAL\n    action: REQUIRE_APPROVAL\n\n  # 権限昇格コマンド\n  privilege_escalation:\n    patterns:\n      - 'sudo|chmod 777|chmod 666'\n      - 'GRANT.*SUPER|ALTER.*ADMIN'\n    severity: HIGH\n    action: REQUIRE_APPROVAL\n```\n\n## 実際に使ってみる：動作確認の手順\n\n### 1. 安全なコード例（パスする）\n\nClaudeに「ユーザー情報を取得するDBクエリを書いて」と頼むと：\n\n```javascript\n// ✅ Code Hooksが許可するコード例\nconst getUser = async (userId) => {\n  const query = 'SELECT id, name, email FROM users WHERE id = ?';\n  const params = [userId];\n  \n  try {\n    const result = await db.query(query, params);\n    return result.rows[0];\n  } catch (error) {\n    console.error('Database error:', error);\n    throw new Error('Failed to fetch user');\n  }\n};\n```\n\n**Code Hooksの判定：✅ パス**\n\n理由：\n- プレースホルダー（?）を使用してSQLインジェクション対策済み\n- 秘密鍵・APIキーなし\n- エラーハンドリング実装済み\n\n### 2. 危険なコード例（ブロックされる）\n\nもしClaudeが（または開発者が）こんなコードを提案したら：\n\n```javascript\n// ❌ Code Hooksがブロックするコード例\nconst password = 'sk_live_abcd1234efgh5678ijkl9012';\n\nconst getUser = (userId) => {\n  const query = `SELECT * FROM users WHERE id = ${userId}`;\n  return db.query(query);\n};\n```\n\n**Code Hooksの判定：❌ BLOCKED**\n\n理由：\n- APIキーがハードコードされている（no-hardcoded-secrets違反）\n- テンプレートリテラルでSQLインジェクション脆弱性（no-sql-injection違反）\n\nClaudeは実行前に以下のメッセージを返します：\n\n```\n⚠️  Code Hooks Security Check Failed\n\n[CRITICAL] Hardcoded secret detected\n  Location: line 1\n  Pattern: API key format detected\n  Fix: Use environment variables instead\n  Example: const password = process.env.API_KEY;\n\n[CRITICAL] SQL Injection vulnerability\n  Location: line 4\n  Pattern: Unparameterized query detected\n  Fix: Use parameterized queries with placeholders\n  Example: db.query('SELECT * FROM users WHERE id = ?', [userId])\n\n実行はブロックされました。上記の修正後、再度お試しください。\n```\n\n### 3. 本番環境への削除操作（承認が必要）\n\nもしClaudeが本番DBから大量削除するマイグレーションを提案した場合：\n\n```sql\n-- ⚠️  Code Hooksが承認確認を要求\nDELETE FROM users WHERE created_at < '2024-01-01';\n```\n\n**Code Hooksの判定：⚠️ REQUIRE_APPROVAL**\n\nClaudeからのメッセージ：\n\n```\n⚠️  Code Hooks Approval Required\n\n[HIGH RISK OPERATION] Production deletion detected\n\n条件：\n  - ブランチ: main（本番環境）\n  - 操作: DELETE FROM users\n  - 影響行数: 推定 ~15,000行\n\n承認方法：\n  1. Claude Code の「承認」ボタンをクリック\n  2. 環境変数 APPROVE_DANGEROUS_OPS=true を設定\n  3. Git commit に [APPROVED] タグを付与\n\nこの操作を本当に実行しますか？\n```\n\nこのとき、開発者は：\n\n- 削除前にバックアップを取る\n- ステージング環境で先にテストする\n- 承認の理由をコメントに残す\n\nという手順を踏むので、うっかり本番環境を壊す確率が大幅に下がるんです。\n\n## よくあるつまずきと解決策\n\n### つまずき1：「Code Hooksが有効になっていない」\n\n**症状：** `.claude/hooks.json` を作ったのに、危ないコードがそのままClaudeに実行されてしまう\n\n**原因：** Claude側が設定ファイルを認識していない\n\n**解決策：**\n\n```bash\n# 1. ファイルの場所を確認\nls -la .claude/hooks.json\nls -la claude.json\n\n# 2. JSONの文法をチェック\njq '.' .claude/hooks.json\n\n# 3. Claude Code の再起動（または新しいチャット開始）\n# Claudeに明示的に伝える\n```\n\nClaudeへの指示文例：\n\n```\n以下のセキュリティルールを有効にしてください：\n\n\"\"\"json\n{\"hooks\": {\"enabled\": true, \"rules\": [...]}}\n\"\"\"\n\nこのプロジェクトではCode Hooksが有効です。\n危ないコードは実行する前に必ず確認してください。\n```\n\n### つまずき2：「false positives（誤検知）が多い」\n\n**症状：** 安全なコードなのに「秘密鍵検知」と誤判定される\n\n**原因：** ルールが過敏に設定されている\n\n**解決策：** ホワイトリスト設定を追加\n\n```json\n{\n  \"hooks\": {\n    \"preExecute\": {\n      \"enabled\": true,\n      \"rules\": [\"no-hardcoded-secrets\"],\n      \"whitelist\": {\n        \"files\": [\n          \"test/**/*.js\",\n          \"docs/**/*\",\n          \"examples/**/*\"\n        ],\n        \"patterns\": [\n          \"test_sk_live_xxx\",\n          \"demo_api_key_xxx\"\n        ]\n      }\n    }\n  }\n}\n```\n\n### つまずき3：「本番環境のブランチ判定がうまくいかない」\n\n**症状：** `production` ブランチなのに、保護ルールが適用されない\n\n**原因：** ブランチ名の認識ズレ\n\n**確認＆修正方法：**\n\n```bash\n# 現在のブランチ名を確認\ngit branch\ngit rev-parse --abbrev-ref HEAD\n\n# hooks.json のbranchリストを更新\n```\n\n```json\n{\n  \"environment\": {\n    \"productionEnv\": [\n      \"main\",\n      \"master\",\n      \"production\",\n      \"prod\",\n      \"release/*\"\n    ]\n  }\n}\n```\n\n## 応用：他のツールとの組み合わせ\n\n### 応用1：GitHubアクションと連携\n\nCode Hooksをローカルだけでなく、CI/CDパイプラインにも組み込む：\n\n```yaml\n# .github/workflows/security-check.yml\nname: Code Hooks Security Check\n\non:\n  pull_request:\n    branches: [main, production

---
※ ハッシュタグ: #AI #初心者
※ 価格設定: ¥0

---

## X導線ポスト（記事投稿後にURLを差し替えてXに投稿）

```
わたし、最近知ったんですけど…Claudeって本番環境での事故を自動で防いでくれるんですね。DB誤操作とか秘密鍵漏洩とか、怖いミスがAIでブロックされるって不思議な感じ。

続きはnoteで詳しく書きました↓
[noteリンク]
```

※ `[noteリンク]` を実際の記事URLに差し替えてください

---
title: Claude CodeでLLMのコンテキストを資産化する｜MCP活用で開発スタイルを根本から変える実装パターン
price: 0
hashtags: AI, 初心者
created_at: 2026-03-15T22:29:53.286105
---

# Claude CodeでLLMのコンテキストを資産化する｜MCP活用で開発スタイルを根本から変える実装パターン

```json
{
  "title": "Claude CodeでLLMのコンテキストを資産化する｜MCP活用で開発スタイルを根本から変える実装パターン",
  "content": "# Claude CodeでLLMのコンテキストを資産化する｜MCP活用で開発スタイルを根本から変える実装パターン\n\n毎回同じ説明をAIに与え直している…そんなことありませんか？わたしが Claude Code を使い始めて気づいたのは、**コンテキストを何度も手入力するのって、すごくもったいない**ってこと。\n\nModel Context Protocol（MCP）を使うと、プロジェクト固有の知識・ルール・コード資産をAIの「頭」に組み込んでしまえます。つまり、開発のたびに一から説明する時代は終わり、AIがあなたのプロジェクト専属アシスタントになるんです。\n\n今日はわたしが実際に試してみてよかった、**コンテキストを資産化する実装パターン**をシェアします。\n\n## なぜコンテキストの資産化が必要なのか\n\nClaude Codeで開発していると、こういう場面に出くわします。\n\n- 「このプロジェクト、うちの命名規則に従ってください」\n- 「データベーススキーマはこれです。毎回張り付けてください」\n- 「これは社内ガイドラインに違反するコード。気をつけてね」\n\n…毎回です。何度も何度も。\n\nわたしも最初は「そりゃしょうがない」と思ってました。でも考えてみると、**同じ情報を何度も入力するって、本質的には時間の無駄**。それに、情報の入力漏れで「あ、ガイドライン確認忘れた」みたいなミスも起きやすくなります。\n\nそこで登場するのが MCP（Model Context Protocol）です。MCPを使うと：\n\n- プロジェクト固有の知識をAIに「組み込める」\n- セッションが変わっても、その知識は保持される\n- 開発チーム全体で同じルール・知識を共有できる\n- コンテキストウィンドウを効率よく使える（毎回の説明で容量を食わない）\n\n結果として、**AIとの対話がシンプルになり、開発スピードが上がる**んです。\n\n## MCPでコンテキストを資産化する、3つの実装パターン\n\n### パターン1：プロジェクト設定をサーバー化する（Basic MCP Server）\n\n最もシンプルなパターンです。プロジェクトの基本設定を MCP サーバーで配信し、Claude Code が常にそれを参照する形。\n\n**ファイル構成：**\n\n```\nmy-project/\n├── mcp-server/\n│   ├── server.js              # MCPサーバーのエントリポイント\n│   ├── resources/\n│   │   ├── naming-guide.md    # 命名規則\n│   │   ├── db-schema.sql      # DBスキーマ\n│   │   └── guidelines.md      # 開発ガイドライン\n│   └── package.json\n├── src/\n│   ├── index.js\n│   └── ...\n└── claude_code_config.json    # Claude Codeの設定\n```\n\n**server.js の実装例：**\n\n```javascript\nconst http = require('http');\nconst fs = require('fs');\nconst path = require('path');\n\nconst resourcesPath = path.join(__dirname, 'resources');\n\n// MCPサーバー（簡易実装）\nconst server = http.createServer((req, res) => {\n  // /resource/[filename] でリソースを返す\n  if (req.url.startsWith('/resource/')) {\n    const filename = req.url.replace('/resource/', '');\n    const filepath = path.join(resourcesPath, filename);\n\n    // パストトラバーサル対策\n    if (!filepath.startsWith(resourcesPath)) {\n      res.writeHead(403);\n      res.end('Forbidden');\n      return;\n    }\n\n    try {\n      const content = fs.readFileSync(filepath, 'utf-8');\n      res.writeHead(200, { 'Content-Type': 'text/plain' });\n      res.end(content);\n    } catch (err) {\n      res.writeHead(404);\n      res.end('Not Found');\n    }\n  } else if (req.url === '/') {\n    // MCPサーバー情報\n    res.writeHead(200, { 'Content-Type': 'application/json' });\n    res.end(JSON.stringify({\n      name: 'Project Context Server',\n      version: '1.0.0',\n      resources: [\n        'naming-guide.md',\n        'db-schema.sql',\n        'guidelines.md'\n      ]\n    }));\n  }\n});\n\nserver.listen(3001, () => {\n  console.log('MCP Server running on http://localhost:3001');\n});\n```\n\n**Claude Code の設定ファイル：**\n\n```json\n{\n  \"mcp_servers\": [\n    {\n      \"name\": \"project-context\",\n      \"url\": \"http://localhost:3001\",\n      \"resources\": [\n        \"naming-guide.md\",\n        \"db-schema.sql\",\n        \"guidelines.md\"\n      ]\n    }\n  ]\n}\n```\n\nこれで Claude Code が起動するたびに、MCP サーバーからリソースを自動取得。**プロジェクトのルール・スキーマが常に最新の状態**で Claude Code に「見えている」状態になります。\n\n### パターン2：チーム知識ベースを JSON で構造化する（Knowledge Base MCP）\n\nより高度なパターンです。命名規則、禁止パターン、推奨アーキテクチャなどを構造化データとして管理。\n\n**knowledge-base.json：**\n\n```json\n{\n  \"project\": {\n    \"name\": \"my-project\",\n    \"version\": \"2.0.0\"\n  },\n  \"naming_conventions\": {\n    \"variables\": {\n      \"rule\": \"camelCase\",\n      \"examples\": [\"userId\", \"fetchUserData\", \"isActive\"]\n    },\n    \"database_columns\": {\n      \"rule\": \"snake_case\",\n      \"examples\": [\"user_id\", \"created_at\", \"is_active\"]\n    },\n    \"constants\": {\n      \"rule\": \"UPPER_SNAKE_CASE\",\n      \"examples\": [\"MAX_RETRY_COUNT\", \"DEFAULT_TIMEOUT\"]\n    }\n  },\n  \"forbidden_patterns\": [\n    {\n      \"pattern\": \"var\",\n      \"reason\": \"Use const or let instead\",\n      \"severity\": \"error\"\n    },\n    {\n      \"pattern\": \"any type in TypeScript\",\n      \"reason\": \"Use proper typing\",\n      \"severity\": \"warning\"\n    }\n  ],\n  \"database_schema\": {\n    \"tables\": [\n      {\n        \"name\": \"users\",\n        \"columns\": [\n          { \"name\": \"id\", \"type\": \"UUID\", \"pk\": true },\n          { \"name\": \"email\", \"type\": \"VARCHAR(255)\", \"unique\": true },\n          { \"name\": \"created_at\", \"type\": \"TIMESTAMP\", \"default\": \"NOW()\" }\n        ]\n      }\n    ]\n  },\n  \"architecture_rules\": [\n    \"Components must be under 300 lines\",\n    \"Services must handle errors with custom exceptions\",\n    \"Database queries must use prepared statements\"\n  ]\n}\n```\n\n**MCPサーバーでこれを処理：**\n\n```javascript\nconst knowledgeBase = require('./knowledge-base.json');\n\nconst server = http.createServer((req, res) => {\n  if (req.url === '/api/schema') {\n    res.writeHead(200, { 'Content-Type': 'application/json' });\n    res.end(JSON.stringify(knowledgeBase.database_schema));\n  } else if (req.url === '/api/naming-rules') {\n    res.writeHead(200, { 'Content-Type': 'application/json' });\n    res.end(JSON.stringify(knowledgeBase.naming_conventions));\n  } else if (req.url === '/api/architecture') {\n    res.writeHead(200, { 'Content-Type': 'application/json' });\n    res.end(JSON.stringify(knowledgeBase.architecture_rules));\n  } else if (req.url === '/api/check-pattern') {\n    const code = req.query?.code || '';\n    const violations = knowledgeBase.forbidden_patterns.filter(p => \n      code.includes(p.pattern)\n    );\n    res.writeHead(200, { 'Content-Type': 'application/json' });\n    res.end(JSON.stringify({ violations }));\n  }\n});\n```\n\nこうすると Claude Code は「ああ、このプロジェクトでは `var` は禁止なんだ」「テーブル構造はこうなってるんだ」と、**構造化された知識として認識**します。\n\n### パターン3：動的コンテキスト更新（Live Context MCP）\n\nプロジェクトが進むにつれて、コンテキストも変わります。例えば、新しいテーブルが追加される、ガイドラインがアップデートされるなど。\n\nこのパターンでは、**Git や webhook を使って自動的にコンテキストを更新**します。\n\n**update-context.js：**\n\n```javascript\nconst { execSync } = require('child_process');\nconst fs = require('fs');\nconst path = require('path');\n\n// 最新のスキーマを取得（例：PostgreSQL）\nfunction updateDatabaseSchema() {\n  try {\n    const schema = execSync(\n      'psql -U user -d mydb -c \"\\\\d\" --tuples-only',\n      { encoding: 'utf-8' }\n    );\n    fs.writeFileSync(\n      path.join(__dirname, 'resources/db-schema-latest.txt'),\n      schema\n    );\n    console.log('Database schema updated');\n  } catch (err) {\n    console.error('Failed to update schema:', err);\n  }\n}\n\n// Git のコミットメッセージから最近の変更を抽出\nfunction extractRecentChanges() {\n  try {\n    const logs = execSync(\n      'git log --oneline --since=\"24 hours ago\"',\n      { encoding: 'utf-8' }\n    );\n    fs.writeFileSync(\n      path.join(__dirname, 'resources/recent-changes.txt'),\n      `Recent commits (24h):\\n${logs}`\n    );\n    console.log('Recent changes updated');\n  } catch (err) {\n    console.error('Failed to update changes:', err);\n  }\n}\n\n// 定期実行（例：毎時間）\nsetInterval(() => {\n  updateDatabaseSchema();\n  extractRecentChanges();\n}, 60 * 60 * 1000);\n\n// 初回実行\nupdateDatabaseSchema();\nextractRecentChanges();\n```\n\nこれを Claude Code の設定に組み込むと、**AIがいつも最新のプロジェクト情報を参照している状態**になります。\n\n## 実装の手順（ステップバイステップ）\n\n### 1. MCPサーバーをセットアップ\n\n```bash\nmkdir -p mcp-server/resources\ncd mcp-server\nnpm init -y\nnpm install express cors\n```\n\n### 2. server.js を実装\n\nパターン1〜3のコード例から、自分のニーズに合ったものを選んで配置。\n\n### 3. リソースファイルを配置\n\n```bash\necho \"# Naming Conventions\" > resources/naming-guide.md\necho \"# Database Schema\" > resources/db-schema.sql\necho \"# Guidelines\" > resources/guidelines.md\n```\n\n### 4. Claude Code の接続設定\n\n Claude Code のプロジェクト設定で、MCP サーバーの URL を指定。設定ファイル（`claude_code_config.json`）またはUI上で MCP サーバーを追加。\n\n### 5. テスト\n\nClaude Code で新しい対話を開始。以下のようにプロンプトを入力：\n\n```\n新しいユーザー取得関数を書いてください。\nプロジェクトのガイドラインと命名規則に従ってください。\n```\n\nAI が自動的に MCP サーバーからリソースを取得し、その内容に基づいて回答するはず。\n\n## よくあるつまずきと解決策\n\n### つまずき1：MCPサーバーが起動しない、または Claude Code が接続できない\n\n**原因**：ポート競合、ファイアウォール設定、URL 指定の誤り\n\n**対策**：\n```bash\n# ポートが使用中か確認\nlsof -i :3001\n\n# 別のポートで起動\nPORT=3002 node server.js\n\n# Claude Code の設定で URL を http://localhost:3002 に変更\n```\n\n### つまずき2：コンテ

---
※ ハッシュタグ: #AI #初心者
※ 価格設定: ¥0

---

## X導線ポスト（記事投稿後にURLを差し替えてXに投稿）

```
わたし最近知ったんですけど、Claude Codeでコンテキストを『資産化』できるって…🌱 MCPを使うと開発スタイルって根本から変わるんですね。詳しくはnoteで

[noteリンク]
```

※ `[noteリンク]` を実際の記事URLに差し替えてください

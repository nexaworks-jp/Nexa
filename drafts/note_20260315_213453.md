---
title: Claude CodeのSkills機能を使いこなす｜カスタムツール開発で業務自動化を実現する実践ガイド
price: 0
hashtags: AI, 初心者
created_at: 2026-03-15T21:23:23.863033
---

# Claude CodeのSkills機能を使いこなす｜カスタムツール開発で業務自動化を実現する実践ガイド

```json
{
  "title": "Claude CodeのSkills機能を使いこなす｜カスタムツール開発で業務自動化を実現する実践ガイド",
  "content": "こんな場面、ありませんか？\n\n毎日同じ定型業務を繰り返している。ちょっとした自動化ツールがあれば、もっと創造的な仕事に時間を使えるのに…\n\nわたしが Claude Code を使い始めたとき、この「ちょっとした自動化」を自分で作れることに気づきました。それが **Skills機能** です。今日はわたしが実際に試してみて、本当に役立った活用方法をシェアします。\n\n## Claude CodeのSkills機能って、そもそも何？\n\nSkills機能は、Claude Codeで作ったカスタムツールを「再利用可能な部品」として登録・保存する仕組みです。\n\nわたしも最初は「単にコードを保存するだけじゃないの？」と思ったんですよね。でも実際に使ってみると、違いました。\n\nSkillsを使うと：\n\n- **何度も呼び出せる**：同じロジックを何回も書き直さなくていい\n- **チーム間で共有可能**：同僚も同じツールを使える\n- **入出力がはっきりしている**：「このツールは何を受け取って何を返すか」が明確になる\n- **保守が簡単**：修正するときは1箇所だけ\n\n特に、データ処理や定期的なチェック作業を自動化したいときに、本当に威力を発揮します。\n\n## なぜSkills機能が必要なのか\n\nわたしが気づいたのは、業務自動化って「一度きり」じゃないってことです。\n\nたとえば：\n- 営業レポートを毎週フォーマットする\n- CSV ファイルを特定のルールで整形する\n- Slack に定期的に通知を送る\n- 複数の API を呼び出して結果をまとめる\n\nこういった作業は、手動でやれば時間がかかるし、単純なスクリプトでやるなら管理が煩雑になります。**Skills機能を使うと、Claude Code の強力さと、再利用性のよさが両立できる** んです。\n\nわたしの場合、営業データの日次チェックツールをSkillsとして作ったら、チーム全体で使ってくれるようになって、みんなの手間が減りました。\n\n## 実際のファイル構成と基本的なSkills作成フロー\n\n一般的な Skills ディレクトリ構成は、こんな感じです：\n\n```\nmy_skills/\n├── skills/\n│   ├── csv_formatter/\n│   │   ├── index.js\n│   │   └── manifest.json\n│   ├── slack_notifier/\n│   │   ├── index.js\n│   │   └── manifest.json\n│   └── data_validator/\n│       ├── index.js\n│       └── manifest.json\n├── tests/\n│   └── skill_tests.js\n└── README.md\n```\n\n各Skillは `manifest.json` と実装ファイル（`index.js` など）から成り立ちます。\n\n### manifest.json の例\n\nSkillの「説明書」になるファイルです：\n\n```json\n{\n  \"name\": \"csv_formatter\",\n  \"version\": \"1.0.0\",\n  \"description\": \"CSVファイルを指定フォーマットに整形する\",\n  \"inputs\": [\n    {\n      \"name\": \"csvData\",\n      \"type\": \"string\",\n      \"description\": \"入力するCSVの文字列\"\n    },\n    {\n      \"name\": \"columnOrder\",\n      \"type\": \"array\",\n      \"description\": \"列の順序 例: ['name', 'email', 'phone']\"\n    }\n  ],\n  \"outputs\": {\n    \"type\": \"object\",\n    \"description\": \"整形済みのデータと変換ログ\"\n  }\n}\n```\n\nこのマニフェストがあると、「このSkillは何を受け取って何を返すのか」が一目瞭然です。\n\n### 実装ファイル（Node.js例）\n\n```javascript\n// skills/csv_formatter/index.js\nconst csv = require('csv-parse/sync');\nconst { stringify } = require('csv-stringify/sync');\n\nmodule.exports = async function formatCSV(inputs) {\n  const { csvData, columnOrder } = inputs;\n  \n  try {\n    // CSVをパース\n    const records = csv.parse(csvData, {\n      columns: true,\n      skip_empty_lines: true\n    });\n    \n    // 指定順序で列を並び替え\n    const formatted = records.map(record => {\n      const reordered = {};\n      columnOrder.forEach(col => {\n        if (record[col] !== undefined) {\n          reordered[col] = record[col];\n        }\n      });\n      return reordered;\n    });\n    \n    // 文字列に変換\n    const output = stringify(formatted, { header: true });\n    \n    return {\n      success: true,\n      data: output,\n      recordCount: formatted.length,\n      log: `${records.length}件をフォーマット完了`\n    };\n  } catch (error) {\n    return {\n      success: false,\n      error: error.message\n    };\n  }\n};\n```\n\n## 実践的なステップバイステップ\n\nわたしが実際にやった流れを書きます。\n\n### ステップ1：Skillの計画を立てる\n\nまず大事なのは「何を自動化したいのか」を明確にすることです。わたしの場合：\n\n- **目的**：顧客データの日次検証\n- **入力**：CSV形式の顧客リスト\n- **処理**：メールアドレスの正合性、電話番号の形式チェック\n- **出力**：エラー箇所を含むレポート\n\nこれを紙に書き出すだけで、後の実装がずっと楽になります。\n\n### ステップ2：manifest.jsonを作成\n\n入出力をはっきり定義します。これが後で複数人で使うときに重要です。\n\n```json\n{\n  \"name\": \"customer_validator\",\n  \"version\": \"1.0.0\",\n  \"description\": \"顧客データの整合性を検証する\",\n  \"inputs\": [\n    {\n      \"name\": \"csvContent\",\n      \"type\": \"string\",\n      \"description\": \"CSVの文字列（ヘッダ: id,name,email,phone）\"\n    }\n  ],\n  \"outputs\": {\n    \"type\": \"object\",\n    \"properties\": {\n      \"validRecords\": { \"type\": \"integer\" },\n      \"invalidRecords\": { \"type\": \"integer\" },\n      \"errors\": { \"type\": \"array\" }\n    }\n  }\n}\n```\n\n### ステップ3：実装する\n\nClaudeとの対話の中で、コードを書きます。「メールアドレスの正合性をチェックしたい」と言えば、適切なロジックを提案してくれます。\n\n```javascript\n// skills/customer_validator/index.js\nconst csv = require('csv-parse/sync');\n\nconst isValidEmail = (email) => {\n  const emailRegex = /^[^\\s@]+@[^\\s@]+\\.[^\\s@]+$/;\n  return emailRegex.test(email);\n};\n\nconst isValidPhone = (phone) => {\n  const phoneRegex = /^\\d{10,11}$/; // 日本の電話番号を想定\n  return phoneRegex.test(phone.replace(/[\\D]/g, ''));\n};\n\nmodule.exports = async function validateCustomers(inputs) {\n  const { csvContent } = inputs;\n  \n  try {\n    const records = csv.parse(csvContent, {\n      columns: true,\n      skip_empty_lines: true\n    });\n    \n    const errors = [];\n    let validCount = 0;\n    \n    records.forEach((record, index) => {\n      const rowErrors = [];\n      \n      if (!isValidEmail(record.email)) {\n        rowErrors.push(`無効なメールアドレス: ${record.email}`);\n      }\n      \n      if (!isValidPhone(record.phone)) {\n        rowErrors.push(`無効な電話番号: ${record.phone}`);\n      }\n      \n      if (rowErrors.length > 0) {\n        errors.push({\n          rowIndex: index + 2, // ヘッダを1行目とする\n          customerId: record.id,\n          issues: rowErrors\n        });\n      } else {\n        validCount++;\n      }\n    });\n    \n    return {\n      success: true,\n      validRecords: validCount,\n      invalidRecords: errors.length,\n      errors: errors,\n      summary: `${validCount}件が有効、${errors.length}件にエラーあり`\n    };\n  } catch (error) {\n    return {\n      success: false,\n      error: error.message\n    };\n  }\n};\n```\n\n### ステップ4：テストを書く\n\nSkillを複数人で使うなら、テストは必須です。\n\n```javascript\n// tests/skill_tests.js\nconst validateCustomers = require('../skills/customer_validator/index.js');\n\nconst testCSV = `id,name,email,phone\n1,山田太郎,yamada@example.com,09012345678\n2,佐藤花子,satou-invalid-email,08987654321\n3,田中次郎,tanaka@example.com,invalid`;\n\n(async () => {\n  const result = await validateCustomers({ csvContent: testCSV });\n  console.log('テスト結果:', JSON.stringify(result, null, 2));\n  \n  // 期待値チェック\n  if (result.validRecords === 1 && result.invalidRecords === 2) {\n    console.log('✓ テスト成功');\n  } else {\n    console.log('✗ テスト失敗');\n  }\n})();\n```\n\n実行：\n\n```bash\nnode tests/skill_tests.js\n```\n\n### ステップ5：Skillを登録・共有する\n\nわたしの環境では、Claude Codeの「Skill Registry」に登録します。これでチームメンバーがそのSkillを使えるようになります。\n\n公式ドキュメントで詳細な登録手順を確認してください。基本的には、フォルダ構造が整っていれば、簡単に登録できます。\n\n## よくあるつまずきと解決策\n\n### 「Skillが見つからない」\n\n**原因**：manifest.json のパスが間違っている、または記述ミスがある\n\n**解決**：\n```bash\n# ファイル構成を確認\nfind ./skills -name manifest.json\n\n# JSONの構文チェック\nnode -e \"console.log(JSON.parse(require('fs').readFileSync('./skills/YOUR_SKILL/manifest.json')))\"\n```\n\nJSONにカンマが抜けていないか、ダブルクォートがちゃんとついているか確認してください。\n\n### 「Skillは見つかるけど、呼び出すと失敗する」\n\n**原因**：入力形式がmanifest.jsonで定義した型と異なっている\n\n**解決**：Claude Codeに「このSkillの inputs を詳しく説明して」と聞いてから、それに合わせて呼び出してください。\n\nたとえば、`csvContent` に配列ではなく文字列を渡しているとか、そういったズレが起きやすいです。\n\n### 「複数のライブラリが必要だけど、環境に入っていない」\n\n**原因**：`package.json` が整備されていない、または依存関係がインストールされていない\n\n**解決**：\n```bash\n# ルートに package.json を作成\ncd my_skills\nnpm init -y\n\n# 必要なパッケージをインストール\nnpm install csv-parse csv-stringify\n\n# インストール後、実行\nnode tests/skill_tests.js\n```\n\n### 「Skillの処理が遅い」\n\n**原因**：大量のレコードを処理する際に、同期的に処理している\n\n**解決**：大規模データの場合は、ストリーミング処理やバッチ処理に切り替えます。ただ、通常の業務自動化レベルなら気にする必要はありません。\n\n## 応用・組み合わせのアイデア\n\nわたしが試してみたら面白かったのが、複数のSkillを組み合わせることです。\n\n### パターン1：Skillチェーン\n\n1番目のSkillの出力を2番目のSkillに入力する\n\n```javascript\n// CSV取得 → 検証 → 整形 → メール送信\nconst csvData = await getCSV()

---
※ ハッシュタグ: #AI #初心者
※ 価格設定: ¥0

---

## X導線ポスト（記事投稿後にURLを差し替えてXに投稿）

```
わたし最近知ったんですけど、Claude Codeの「Skills機能」を使うと、自分だけのカスタムツールが作れるんです。業務自動化がぐっと現実的になる感じがして。

続きはnoteで👇
[noteリンク]
```

※ `[noteリンク]` を実際の記事URLに差し替えてください

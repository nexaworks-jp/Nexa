---
title: Claude Codeのエージェント機能を使いこなす｜複数AIの協働で何ができるか【実践ガイド】
price: 0
hashtags: AI, 初心者
created_at: 2026-03-14T18:25:24.685016
---

# Claude Codeのエージェント機能を使いこなす｜複数AIの協働で何ができるか【実践ガイド】

```json
{
  "title": "Claude Codeのエージェント機能を使いこなす｜複数AIの協働で何ができるか【実践ガイド】",
  "content": "# Claude Codeのエージェント機能を使いこなす｜複数AIの協働で何ができるか【実践ガイド】\n\n「複数のAIに同時に仕事をさせたら、もっと複雑な問題も解けるんじゃないか……」\n\nわたしがClaudeを使い込んでいくうちに、そんなことを考え始めました。実は、Claude Codeのエージェント機能を使うと、複数のAI（やツール）を組み合わせて、ひとつのAIでは解けない問題を協働で解くことができるんです。\n\nこの記事では、わたしが実際に試してみてうまくいった「複数AIの協働」パターンと、実装の具体的な流れをシェアします。エンジニアなら「あ、これなら自分の開発フローにも組み込めそう」と感じる内容になっていると思います。\n\n## エージェント機能とは何か、なぜ必要なのか\n\nわたしが最初に戸惑ったのは、「エージェント」という言葉自体でした。何がダメなんだろ！！と思いながら調べていたんですが、シンプルに言うと：\n\n**エージェント = 複数のツール・AIを使い分けて、目標を達成するまで自律的に動作するシステム**\n\nたとえば、プロジェクトの進捗レポートを自動生成したいとしましょう。\n\n- GitHubから最新のコミット履歴を取得（ツール1）\n- Jiraのタスク情報を集計（ツール2）\n- その情報をまとめてレポートを生成（Claude）\n- 結果が正しいかチェック（別のAI）\n\nこれらを「人間が指示するたびに」やるのではなく、**エージェントが自分で判断して、順番に実行していく**のがポイントです。\n\n何が嬉しいかというと：\n\n1. **複数ツールの連携を自動化できる**：手動でAPI叩いたり、結果をコピペする手間が消える\n2. **問題解決が早くなる**：各ツールの専門性を活かせるから、単一のAIより精度が高い\n3. **スケーラビリティ**：一度仕組みを作れば、同じパターンの問題は大量処理できる\n4. **AIの「考える過程」を見える化できる**：なぜそう判断したのかがログに残る\n\n## 実際のファイル構成とコード例\n\nわたしが実際に動かしてみた構成です。プロジェクト管理システムのタスク自動分類を例に説明します。\n\n```\nagent-project/\n├── agents/\n│   ├── __init__.py\n│   ├── base_agent.py          # エージェントの基底クラス\n│   ├── task_classifier.py     # タスク分類エージェント\n│   ├── priority_evaluator.py  # 優先度判定エージェント\n│   └── reporter.py            # レポート生成エージェント\n├── tools/\n│   ├── __init__.py\n│   ├── github_tool.py         # GitHub API ラッパー\n│   ├── jira_tool.py           # Jira API ラッパー\n│   └── slack_tool.py          # Slack通知ツール\n├── config/\n│   └── settings.py            # 環境変数・設定\n├── main.py                    # エントリーポイント\n└── requirements.txt\n```\n\n### 基底エージェントクラス\n\n```python\n# agents/base_agent.py\nfrom anthropic import Anthropic\nfrom typing import Optional, List, Dict, Any\nimport json\n\nclass BaseAgent:\n    def __init__(self, model: str = \"claude-opus-4-1\", temperature: float = 0.7):\n        self.client = Anthropic()\n        self.model = model\n        self.temperature = temperature\n        self.conversation_history = []\n        self.tools = []\n    \n    def register_tool(self, tool: Dict[str, Any]):\n        \"\"\"ツールをエージェントに登録\"\"\"\n        self.tools.append(tool)\n    \n    def add_message(self, role: str, content: str):\n        \"\"\"会話履歴にメッセージを追加\"\"\"\n        self.conversation_history.append({\n            \"role\": role,\n            \"content\": content\n        })\n    \n    def call_claude(self, user_input: str) -> str:\n        \"\"\"Claudeを呼び出す（ツール使用対応）\"\"\"\n        self.add_message(\"user\", user_input)\n        \n        # ツールがある場合とない場合を分岐\n        kwargs = {\n            \"model\": self.model,\n            \"max_tokens\": 2048,\n            \"temperature\": self.temperature,\n            \"messages\": self.conversation_history,\n        }\n        \n        if self.tools:\n            kwargs[\"tools\"] = self.tools\n        \n        response = self.client.messages.create(**kwargs)\n        \n        # レスポンスをパース\n        if response.stop_reason == \"tool_use\":\n            return self._handle_tool_use(response)\n        else:\n            assistant_message = response.content[0].text\n            self.add_message(\"assistant\", assistant_message)\n            return assistant_message\n    \n    def _handle_tool_use(self, response) -> str:\n        \"\"\"ツール使用リクエストを処理\"\"\"\n        # わたしが実装した時点では、tool_useブロックを見つけてツールを実行\n        tool_results = []\n        assistant_content = response.content\n        \n        for block in response.content:\n            if block.type == \"tool_use\":\n                tool_name = block.name\n                tool_input = block.input\n                tool_use_id = block.id\n                \n                # ツール実行（サブクラスで実装）\n                result = self.execute_tool(tool_name, tool_input)\n                tool_results.append({\n                    \"type\": \"tool_result\",\n                    \"tool_use_id\": tool_use_id,\n                    \"content\": json.dumps(result) if isinstance(result, dict) else str(result)\n                })\n        \n        # アシスタントメッセージを履歴に追加\n        self.add_message(\"assistant\", response.content)\n        \n        # ツール結果をメッセージとして追加\n        self.add_message(\"user\", tool_results)\n        \n        # 再度Claudeに問い合わせ（ツール結果を踏まえての判断）\n        response = self.client.messages.create(\n            model=self.model,\n            max_tokens=2048,\n            messages=self.conversation_history,\n            tools=self.tools if self.tools else None\n        )\n        \n        if response.stop_reason == \"tool_use\":\n            return self._handle_tool_use(response)  # 再帰的に処理\n        else:\n            final_message = response.content[0].text\n            self.add_message(\"assistant\", final_message)\n            return final_message\n    \n    def execute_tool(self, tool_name: str, tool_input: Dict) -> Any:\n        \"\"\"ツールを実行（サブクラスで実装）\"\"\"\n        raise NotImplementedError(\"Subclass must implement execute_tool\")\n```\n\n### タスク分類エージェント\n\n```python\n# agents/task_classifier.py\nfrom .base_agent import BaseAgent\nfrom tools.github_tool import GitHubTool\nimport json\n\nclass TaskClassifier(BaseAgent):\n    def __init__(self):\n        super().__init__()\n        self.github_tool = GitHubTool()\n        self._register_tools()\n    \n    def _register_tools(self):\n        \"\"\"分類用のツールを登録\"\"\"\n        tools = [\n            {\n                \"name\": \"fetch_github_issues\",\n                \"description\": \"GitHubのリポジトリからオープンなissueを取得\",\n                \"input_schema\": {\n                    \"type\": \"object\",\n                    \"properties\": {\n                        \"owner\": {\"type\": \"string\", \"description\": \"リポジトリオーナー\"},\n                        \"repo\": {\"type\": \"string\", \"description\": \"リポジトリ名\"},\n                        \"state\": {\"type\": \"string\", \"description\": \"open または closed\"}\n                    },\n                    \"required\": [\"owner\", \"repo\"]\n                }\n            },\n            {\n                \"name\": \"classify_issue\",\n                \"description\": \"issueをカテゴリーと優先度に分類\",\n                \"input_schema\": {\n                    \"type\": \"object\",\n                    \"properties\": {\n                        \"issue_title\": {\"type\": \"string\"},\n                        \"issue_body\": {\"type\": \"string\"},\n                        \"labels\": {\"type\": \"array\", \"items\": {\"type\": \"string\"}}\n                    },\n                    \"required\": [\"issue_title\", \"issue_body\"]\n                }\n            }\n        ]\n        \n        for tool in tools:\n            self.register_tool(tool)\n    \n    def execute_tool(self, tool_name: str, tool_input: dict):\n        \"\"\"ツール実行の本体\"\"\"\n        if tool_name == \"fetch_github_issues\":\n            return self.github_tool.fetch_issues(\n                tool_input[\"owner\"],\n                tool_input[\"repo\"],\n                tool_input.get(\"state\", \"open\")\n            )\n        elif tool_name == \"classify_issue\":\n            # わたしがテストしたときは、タイトルと本文からカテゴリーを推論\n            return {\n                \"category\": \"feature\" if \"feature\" in tool_input[\"issue_title\"].lower() else \"bug\",\n                \"priority\": \"high\" if \"urgent\" in tool_input[\"issue_body\"].lower() else \"normal\"\n            }\n    \n    def classify_workflow(self, owner: str, repo: str) -> dict:\n        \"\"\"分類ワークフローを実行\"\"\"\n        prompt = f\"\"\"\nGitHubリポジトリ {owner}/{repo} のissueを取得して、\n以下の手順で分類してください：\n\n1. fetch_github_issues ツールでissueを取得\n2. 各issueに対して classify_issue ツールで分類\n3. 分類結果をJSON形式でまとめる\n\n分類のカテゴリー：feature, bug, documentation, refactor\n優先度：high, normal, low\n        \"\"\"\n        \n        result = self.call_claude(prompt)\n        return json.loads(result) if result.startswith(\"{\") else {\"raw_result\": result}\n```\n\n### メインの協働エージェント実行\n\n```python\n# main.py\nfrom agents.task_classifier import TaskClassifier\nfrom agents.priority_evaluator import PriorityEvaluator\nfrom agents.reporter import Reporter\nimport json\n\ndef run_collaborative_workflow():\n    \"\"\"複数のエージェントを協働させる\"\"\"\n    \n    print(\"\\n=== Step 1: タスク分類 ===\")\n    classifier = TaskClassifier()\n    classified_tasks = classifier.classify_workflow(\"anthropics\", \"anthropic-sdk-python\")\n    print(f\"分類結果: {json.dumps(classified_tasks, indent=2)}\")  # わたしが実装したときはこれでデバッグ\n    \n    print(\"\\n=== Step 2: 優先度評価 ===\")\n    evaluator = PriorityEvaluator()\n    # 分類結果を優先度評価エージェントに渡す\n    evaluated_tasks = evaluator.evaluate_priority(classified_tasks)\n    print(f\"優先度評価: {json.dumps(evaluated_tasks, indent=2)}\")\n    \n    print(\"\\n=== Step 3: レポート生成 ===\")\n    reporter = Reporter()\n    # 最終的なレポートを生成\n    report = reporter.generate_report(evaluated_tasks)\n    print(f\"最終レポート:\\n{report}\")\n    \n    return report\n\nif __name__ == \"__main__\":\n    run_collaborative_workflow()\n```\n\n## 手順ステップバイステップ\n\n### 1. 環境構築と依存関係の設定\n\n```bash\npip install anthropic python-dotenv requests\n```\n\n`.env` ファイルを作成：\n\n```\nANTHROPIC_API_KEY=your_api_key_here\nGITHUB_TOKEN=your_github_token_here\nJIRA_URL=https://your-jira-instance.atlassian.net\nJIRA_EMAIL=your-email@example.com\nJIRA_TOKEN=your_jira_token_here\n```\n\n### 2. ツール層の実装\n\nわたしが実装した時に気をつけたのは、**各ツールを独立させる**ことです。こうすることで、あるツールの変更が他のエージェントに影響しません。\n\n```python\n# tools/github_tool.py\nimport os\nimport requests\n\nclass GitHubTool:\n    def __init__(self):\

---
※ ハッシュタグ: #AI #初心者
※ 価格設定: ¥0

---

## X導線ポスト（記事投稿後にURLを差し替えてXに投稿）

```
わたし、最近知ったんですけど…複数のAIが一緒に動くって、こんなに便利なんですね。Claude Codeのエージェント機能で何ができるか、実際に試してみた話を書きました。

[noteリンク]
```

※ `[noteリンク]` を実際の記事URLに差し替えてください

---
title: Playwright + OWASP ZAP + Claude Codeで脆弱性診断を自動化する｜セキュアな開発フロー構築ガイド
price: 0
hashtags: AI, 初心者
created_at: 2026-03-14T17:58:14.284759
---

# Playwright + OWASP ZAP + Claude Codeで脆弱性診断を自動化する｜セキュアな開発フロー構築ガイド

```json
{
  "title": "Playwright + OWASP ZAP + Claude Codeで脆弱性診断を自動化する｜セキュアな開発フロー構築ガイド",
  "content": "🔒\n\n## こんなシーンで活躍します\n\nWebアプリケーション開発をしていて、セキュリティテストが後回しになっていませんか？わたしが開発フローを見直してみたら、脆弱性診断って「本番前の最後にやる作業」じゃなくて、「開発の途中で何度も繰り返すべきもの」なんだと気づきました。\n\nでも手動で毎回テストするのは時間がかかるし、見落としも増える。そこで気づいたのが、Playwright（ブラウザ自動化）+ OWASP ZAP（脆弱性スキャナ）+ Claude Code（AIコード生成）の組み合わせです。この3つを繋ぐと、開発フローの中に脆弱性診断を自動で埋め込める。\n\n今日は、わたしが実際に試してうまくいったセットアップとコード例をシェアします。\n\n---\n\n## なぜこの組み合わせなのか\n\nそれぞれのツールの役割を整理しておくと、次のステップが理解しやすくなります。\n\n**Playwright** は、ブラウザを自動操作して「実際のユーザー操作をシミュレート」します。ログイン画面を入力する、フォームを送信する、複数のページを移動する――こうした一連の動作を自動化できます。\n\n**OWASP ZAP** は、セキュリティテストに特化したスキャナです。Playwrightで生成されたトラフィックを「見張って」、SQLインジェクション、XSS、CSRF、認証バイパスといった既知の脆弱性パターンを検出します。\n\n**Claude Code** は、この2つを繋ぐロジックを「自分で考えながら」生成します。手順書を読まなくても、「Playwrightでこういう操作をして、その結果をZAPに送ってレポート化して」という要望を書くと、実行可能なコードが出てきます。\n\nわたしが最初に手動で繋いでみたときは、シェルスクリプトとPythonが混在してかなり複雑でした。でもClaude Codeに「このフローを整理して」と投げると、構造化されたコードに生まれ変わった。それが便利だったんです。\n\n---\n\n## 実際のファイル構成とコード例\n\nまず、プロジェクト全体の構成をこんな感じで作ります。\n\n```\nproject-root/\n├── playwright/\n│   ├── tests/\n│   │   ├── login.spec.ts\n│   │   ├── user-flow.spec.ts\n│   │   └── admin-panel.spec.ts\n│   ├── playwright.config.ts\n│   └── package.json\n├── zap/\n│   ├── zap-config.yaml\n│   ├── context-files/\n│   │   └── app-context.context\n│   └── scripts/\n│       └── run-scan.py\n├── automation/\n│   ├── main.py\n│   ├── playwright-runner.py\n│   ├── zap-runner.py\n│   ├── report-generator.py\n│   └── requirements.txt\n└── reports/\n    ├── playwright-results/\n    └── zap-results/\n```\n\nこの構成にした理由は、Playwrightとzapをそれぞれ独立した単位で管理しつつ、`automation`フォルダ内でそれらを呼び出すオーケストレータを作るため。Claude Codeでコードを生成するとき、この分離があると「何をやっているのか」が明確になるんです。\n\n### Playwrightのテスト例\n\n```typescript\n// playwright/tests/login.spec.ts\nimport { test, expect } from '@playwright/test';\n\ntest.describe('Security Test - Login Flow', () => {\n  test('should login with valid credentials', async ({ page }) => {\n    await page.goto('http://localhost:8080/login');\n    \n    await page.fill('input[name=\"username\"]', 'testuser');\n    await page.fill('input[name=\"password\"]', 'testpass123');\n    await page.click('button[type=\"submit\"]');\n    \n    // ログイン成功を確認\n    await expect(page).toHaveURL('http://localhost:8080/dashboard');\n    \n    // ここはセッションを保持したままZAPスキャンへ\n  });\n\n  test('should test sql injection in search', async ({ page }) => {\n    await page.goto('http://localhost:8080/login');\n    await page.fill('input[name=\"username\"]', 'testuser');\n    await page.fill('input[name=\"password\"]', 'testpass123');\n    await page.click('button[type=\"submit\"]');\n    \n    // 検索フォームに到達\n    await page.goto('http://localhost:8080/search');\n    await page.fill('input[name=\"q\"]', \"' OR '1'='1\");\n    await page.click('button[type=\"submit\"]');\n    \n    // ここでZAPがトラフィックを監視している\n  });\n});\n```\n\n### Python自動化スクリプト\n\n```python\n# automation/main.py\nimport subprocess\nimport json\nimport time\nfrom datetime import datetime\nfrom pathlib import Path\nfrom playwright_runner import run_playwright_tests\nfrom zap_runner import run_zap_scan, get_zap_results\nfrom report_generator import generate_html_report\n\ndef main():\n    print(\"[Step 1] Starting Playwright automated tests...\")\n    playwright_result = run_playwright_tests(\n        test_dir=\"playwright/tests\",\n        output_file=\"reports/playwright-results/results.json\"\n    )\n    \n    if not playwright_result['success']:\n        print(\"❌ Playwright tests failed. Aborting ZAP scan.\")\n        return\n    \n    print(\"✅ Playwright tests completed successfully.\")\n    print(f\"Generated HAR file at: {playwright_result['har_file']}\")\n    \n    # ZAPが古い状態だと結果がおかしくなるので少し待つ\n    time.sleep(2)\n    \n    print(\"\\n[Step 2] Starting OWASP ZAP scan...\")\n    zap_result = run_zap_scan(\n        target_url=\"http://localhost:8080\",\n        context_file=\"zap/context-files/app-context.context\",\n        output_file=\"reports/zap-results/zap-report.json\"\n    )\n    \n    if not zap_result['success']:\n        print(\"⚠️  ZAP scan encountered issues.\")\n        print(zap_result['error'])\n    \n    print(\"✅ ZAP scan completed.\")\n    print(f\"Vulnerabilities found: {zap_result['vulnerability_count']}\")\n    \n    print(\"\\n[Step 3] Generating unified report...\")\n    report_file = generate_html_report(\n        playwright_results=playwright_result,\n        zap_results=zap_result,\n        output_file=f\"reports/security-report-{datetime.now().strftime('%Y%m%d-%H%M%S')}.html\"\n    )\n    \n    print(f\"✅ Report generated: {report_file}\")\n    print(\"\\n--- Summary ---\")\n    print(f\"Playwright tests: {playwright_result['passed']}/{playwright_result['total']}\")\n    print(f\"ZAP vulnerabilities: High={zap_result['high']}, Medium={zap_result['medium']}, Low={zap_result['low']}\")\n\nif __name__ == \"__main__\":\n    main()\n```\n\n### Playwright実行モジュール\n\n```python\n# automation/playwright-runner.py\nimport subprocess\nimport json\nfrom pathlib import Path\n\ndef run_playwright_tests(test_dir: str, output_file: str) -> dict:\n    \"\"\"\n    Playwrightテストを実行して結果を返す\n    \"\"\"\n    output_path = Path(output_file)\n    output_path.parent.mkdir(parents=True, exist_ok=True)\n    \n    try:\n        # Playwrightテストを実行（JSON形式で出力）\n        result = subprocess.run(\n            [\n                \"npx\", \"playwright\", \"test\",\n                test_dir,\n                \"--reporter=json\",\n                f\"--output-file={output_file}\"\n            ],\n            capture_output=True,\n            text=True,\n            timeout=300\n        )\n        \n        # 結果をパース\n        with open(output_file, 'r') as f:\n            test_results = json.load(f)\n        \n        total = len(test_results['suites'][0]['tests']) if test_results.get('suites') else 0\n        passed = sum(1 for s in test_results.get('suites', []) \n                     for t in s.get('tests', []) if t['status'] == 'passed')\n        \n        return {\n            'success': result.returncode == 0,\n            'total': total,\n            'passed': passed,\n            'har_file': None,  # 必要に応じてHARファイル生成ロジックを追加\n            'output_file': output_file\n        }\n    \n    except subprocess.TimeoutExpired:\n        return {\n            'success': False,\n            'error': 'Playwright tests timed out',\n            'total': 0,\n            'passed': 0\n        }\n    except Exception as e:\n        return {\n            'success': False,\n            'error': str(e),\n            'total': 0,\n            'passed': 0\n        }\n```\n\n### ZAP実行モジュール\n\n```python\n# automation/zap-runner.py\nimport subprocess\nimport json\nimport time\nfrom pathlib import Path\n\ndef run_zap_scan(target_url: str, context_file: str, output_file: str) -> dict:\n    \"\"\"\n    OWASP ZAPでセキュリティスキャンを実行\n    \"\"\"\n    output_path = Path(output_file)\n    output_path.parent.mkdir(parents=True, exist_ok=True)\n    \n    try:\n        # ZAP APIを叩くコマンド（またはPythonバインディング）\n        # ここは環境に応じて変更\n        cmd = [\n            \"zaproxy\",\n            \"-cmd\",\n            \"-quickurl\", target_url,\n            \"-quickout\", output_file,\n            \"-contextfile\", context_file\n        ]\n        \n        result = subprocess.run(\n            cmd,\n            capture_output=True,\n            text=True,\n            timeout=600\n        )\n        \n        # ZAP出力をパース\n        try:\n            with open(output_file, 'r') as f:\n                zap_report = json.load(f)\n        except json.JSONDecodeError:\n            # XML形式の場合はパース処理を追加\n            zap_report = {'success': False}\n        \n        # 脆弱性レベル別にカウント\n        alerts = zap_report.get('site', [{}])[0].get('alerts', [])\n        high_count = sum(1 for a in alerts if a.get('riskcode') == '3')\n        medium_count = sum(1 for a in alerts if a.get('riskcode') == '2')\n        low_count = sum(1 for a in alerts if a.get('riskcode') == '1')\n        \n        return {\n            'success': result.returncode == 0,\n            'vulnerability_count': len(alerts),\n            'high': high_count,\n            'medium': medium_count,\n            'low': low_count,\n            'output_file': output_file\n        }\n    \n    except subprocess.TimeoutExpired:\n        return {\n            'success': False,\n            'error': 'ZAP scan timed out',\n            'vulnerability_count': 0,\n            'high': 0, 'medium': 0, 'low': 0\n        }\n    except Exception as e:\n        return {\n            'success': False,\n            'error': str(e),\n            'vulnerability_count': 0,\n            'high': 0, 'medium': 0, 'low': 0\n        }\n```\n\n---\n\n## ステップバイステップ実装ガイド\n\n### 1. 環境セットアップ\n\n```bash\n# Node.js環境（Playwright用）\nmkdir -p playwright\ncd playwright\nnpm init -y\nnpm install --save-dev @playwright/test\nnpx playwright install\ncd ..\n\n# Python環境\npython3 -m venv venv\nsource venv/bin/activate  # Windows: venv\\Scripts\\activate\n\ncd automation\npip install -r requirements.txt\n```\n\n`automation/requirements.txt`の内容：\n```\nrequests==2.31.0\npython-owasp-zap==0.0.20\npytest==7.4.0\nPyYAML==6.0\n```\n\n### 2. Playwrightテストファイルの作成\n\nわたしが実際に書いてみたときに気をつけたポイント：\n\n- **ローカルホスト指定**：開発環境のURLに合わせる\n- **遅延を入れる**：ページの読み込み完了を待つ\n- **複数シナリ

---
※ ハッシュタグ: #AI #初心者
※ 価格設定: ¥0

---

## X導線ポスト（記事投稿後にURLを差し替えてXに投稿）

```
わたし最近知ったんですけど、脆弱性診断をClaudeに任せちゃう方法があるんです。Playwrightで自動テスト、OWASP ZAPでスキャン、Claude Codeで結果を整理…セキュリティ確認が一気に楽になる 🔍

続きはnoteで→
[noteリンク]
```

※ `[noteリンク]` を実際の記事URLに差し替えてください

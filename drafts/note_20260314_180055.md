---
title: Claude Codeで自分好みの朝刊配信システムを作る｜実践的なAPI連携ガイド
price: 0
hashtags: AI, 初心者
created_at: 2026-03-14T17:55:17.646922
---

# Claude Codeで自分好みの朝刊配信システムを作る｜実践的なAPI連携ガイド

```json
{
  "title": "Claude Codeで自分好みの朝刊配信システムを作る｜実践的なAPI連携ガイド",
  "content": "# Claude Codeで自分好みの朝刊配信システムを作る｜実践的なAPI連携ガイド\n\n毎朝、自分が本当に読みたいニュースだけを、決まった時間にメールで受け取れたら…そんなことを思ったことはありませんか？\n\nわたしも最初は「それってプログラミングの知識がないと難しいのでは」と思っていたのですが、Claude Codeを使うと、そこまで複雑ではないことに気づきました。NewsAPIのような無料のニュースAPI、メール送信API、そしてスケジューリング機能を組み合わせるだけで、自分専用の朝刊システムが作れてしまいます。\n\n今日はわたしが実際に試して「これなら動く」と確認できた、朝刊配信システムの作り方をシェアします。\n\n## なぜ朝刊配信システムなのか・何が嬉しいのか\n\nちょっと違う視点から見ると、朝刊配信システムって「APIの基本を学ぶ絶好の教材」なんですよ。\n\n- **複数のAPI連携が必要** → APIキーの管理・リクエスト・レスポンス処理を実践できる\n- **条件付きフィルタリング** → 自分の興味キーワードだけを抽出するロジックを学べる\n- **定期実行** → Cronやタスクスケジューラなどのスケジューリング概念に触れられる\n- **エラーハンドリングが重要** → 実務的なエラー対応を意識する必要がある\n\nつまり、趣味的に見えて実は「実務で求められるスキル」がギュッと詰まっているんです。\n\nそのうえ、完成すればあなたの毎朝が少し快適になる。これ以上の学習教材ってないですよね。\n\n## 必要なもの（事前準備）\n\n本格的に進める前に、以下を用意しておくと進みがスムーズです。\n\n1. **NewsAPI のAPIキー** → https://newsapi.org で無料登録（毎日100リクエストまで無料）\n2. **メール送信API** → SendGridまたはGmail API（SendGridは月100通まで無料）\n3. **ローカル環境** → Python 3.8 以上がインストールされた環境\n4. **タスク実行環境** → Cronが使える Linux/Mac、またはWindowsのタスクスケジューラ\n\nNewsAPI のキーがあると、実際にニュース記事を取得できるので、動作確認がしやすくなります。\n\n## 実際のファイル構成とコード例\n\nまず、全体のファイル構成を見ておきましょう。\n\n```\nmorning-news-system/\n├── main.py                 # メインのスクリプト\n├── config.py               # API キーなど設定\n├── news_fetcher.py         # ニュース取得ロジック\n├── email_sender.py         # メール送信ロジック\n├── filter_keywords.py      # フィルタリング設定\n├── requirements.txt        # パッケージ依存関係\n└── logs/\n    └── delivery.log        # 実行ログ\n```\n\n### 1. config.py（設定ファイル）\n\nAPIキーや設定をここで一元管理します。\n\n```python\nimport os\nfrom dotenv import load_dotenv\n\nload_dotenv()  # .envファイルから環境変数を読み込む\n\n# ニュースAPI設定\nNEWS_API_KEY = os.getenv(\"NEWS_API_KEY\")\nNEWS_API_URL = \"https://newsapi.org/v2/top-headlines\"\n\n# メール送信API設定（SendGridの例）\nSENDGRID_API_KEY = os.getenv(\"SENDGRID_API_KEY\")\nSENDER_EMAIL = os.getenv(\"SENDER_EMAIL\")\nRECIPIENT_EMAIL = os.getenv(\"RECIPIENT_EMAIL\")\n\n# ニュースフィルタリング設定\nINTEREST_KEYWORDS = [\"テクノロジー\", \"AI\", \"スタートアップ\", \"データ\"]\nCOUNTRY = \"jp\"  # 日本のニュースに限定\nLANGUAGE = \"ja\"\n\n# ログ設定\nLOG_FILE = \"logs/delivery.log\"\n```\n\n### 2. news_fetcher.py（ニュース取得）\n\nNewsAPI から実際にニュースを取得するコンポーネント。\n\n```python\nimport requests\nfrom datetime import datetime, timedelta\nimport logging\nfrom config import NEWS_API_KEY, NEWS_API_URL, COUNTRY, LANGUAGE\n\nlogging.basicConfig(level=logging.INFO)\nlogger = logging.getLogger(__name__)\n\ndef fetch_news():\n    \"\"\"\n    NewsAPI からニュースを取得\n    \"\"\"\n    try:\n        # 24時間以内のニュースを取得\n        params = {\n            \"country\": COUNTRY,\n            \"apiKey\": NEWS_API_KEY,\n            \"sortBy\": \"publishedAt\",\n            \"pageSize\": 50\n        }\n        \n        response = requests.get(NEWS_API_URL, params=params, timeout=10)\n        response.raise_for_status()  # ステータスコードエラーをチェック\n        \n        data = response.json()\n        \n        if data[\"status\"] != \"ok\":\n            logger.error(f\"API Error: {data.get('message', 'Unknown error')}\")\n            return []\n        \n        articles = data.get(\"articles\", [])\n        logger.info(f\"取得したニュース件数: {len(articles)}\")\n        \n        return articles\n    \n    except requests.exceptions.RequestException as e:\n        logger.error(f\"ネットワークエラー: {e}\")\n        return []\n    except Exception as e:\n        logger.error(f\"予期しないエラー: {e}\")\n        return []\n\ndef filter_by_keywords(articles, keywords):\n    \"\"\"\n    キーワードでニュースをフィルタリング\n    \"\"\"\n    filtered = []\n    \n    for article in articles:\n        title = article.get(\"title\", \"\").lower()\n        description = article.get(\"description\", \"\").lower()\n        content = f\"{title} {description}\"\n        \n        # キーワードが1つ以上含まれているかチェック\n        if any(keyword.lower() in content for keyword in keywords):\n            filtered.append(article)\n    \n    logger.info(f\"フィルタ後のニュース件数: {len(filtered)}\")\n    return filtered\n```\n\n### 3. email_sender.py（メール送信）\n\nSendGrid を使ってメール配信。\n\n```python\nfrom sendgrid import SendGridAPIClient\nfrom sendgrid.helpers.mail import Mail, Content, Email\nimport logging\nfrom config import SENDGRID_API_KEY, SENDER_EMAIL, RECIPIENT_EMAIL\n\nlogger = logging.getLogger(__name__)\n\ndef send_news_email(articles):\n    \"\"\"\n    ニュース記事をメールで送信\n    \"\"\"\n    if not articles:\n        logger.info(\"送信するニュースがありません\")\n        return False\n    \n    try:\n        # メール本文を生成\n        body_html = generate_email_html(articles)\n        \n        # メールオブジェクトを作成\n        mail = Mail(\n            from_email=SENDER_EMAIL,\n            to_emails=RECIPIENT_EMAIL,\n            subject=\"📰 今朝のニュース配信\",\n            html_content=body_html\n        )\n        \n        # SendGrid経由で送信\n        sg = SendGridAPIClient(SENDGRID_API_KEY)\n        response = sg.send(mail)\n        \n        logger.info(f\"メール送信成功 (ステータス: {response.status_code})\")\n        return True\n    \n    except Exception as e:\n        logger.error(f\"メール送信失敗: {e}\")\n        return False\n\ndef generate_email_html(articles):\n    \"\"\"\n    HTML形式のメール本文を生成\n    \"\"\"\n    html = \"<h1>📰 今朝のニュース配信</h1>\\n\"\n    html += f\"<p>配信時刻: {datetime.now().strftime('%Y年%m月%d日 %H:%M:%S')}</p>\\n\"\n    html += \"<hr>\\n\"\n    \n    for idx, article in enumerate(articles[:10], 1):  # 最大10件\n        title = article.get(\"title\", \"\")\n        description = article.get(\"description\", \"\")\n        url = article.get(\"url\", \"\")\n        source = article.get(\"source\", {}).get(\"name\", \"\")\n        published_at = article.get(\"publishedAt\", \"\")\n        \n        html += f\"<h3>{idx}. {title}</h3>\\n\"\n        html += f\"<p>{description}</p>\\n\"\n        html += f\"<p><small>📌 {source} | {published_at}</small></p>\\n\"\n        html += f\"<p><a href=\\\"{url}\\\">記事を読む →</a></p>\\n\"\n        html += \"<hr>\\n\"\n    \n    return html\n```\n\n### 4. main.py（メインスクリプト）\n\n各コンポーネントを統合して実行。\n\n```python\nimport logging\nfrom datetime import datetime\nfrom news_fetcher import fetch_news, filter_by_keywords\nfrom email_sender import send_news_email\nfrom filter_keywords import INTEREST_KEYWORDS\nfrom config import LOG_FILE\n\n# ログ設定\nlogging.basicConfig(\n    level=logging.INFO,\n    format='%(asctime)s - %(name)s - %(levelname)s - %(message)s',\n    handlers=[\n        logging.FileHandler(LOG_FILE),\n        logging.StreamHandler()\n    ]\n)\nlogger = logging.getLogger(__name__)\n\ndef main():\n    \"\"\"\n    朝刊配信システムのメイン処理\n    \"\"\"\n    logger.info(\"=\" * 50)\n    logger.info(\"朝刊配信システム 実行開始\")\n    logger.info(\"=\" * 50)\n    \n    try:\n        # 1. ニュースを取得\n        logger.info(\"ニュース取得中...\")\n        articles = fetch_news()\n        \n        if not articles:\n            logger.warning(\"ニュースが取得できませんでした\")\n            return False\n        \n        # 2. キーワードでフィルタリング\n        logger.info(f\"キーワード: {INTEREST_KEYWORDS}\")\n        filtered_articles = filter_by_keywords(articles, INTEREST_KEYWORDS)\n        \n        if not filtered_articles:\n            logger.info(\"該当するニュースがありません\")\n            return False\n        \n        # 3. メール送信\n        logger.info(\"メール送信中...\")\n        success = send_news_email(filtered_articles)\n        \n        if success:\n            logger.info(\"朝刊配信システム 実行完了\")\n            return True\n        else:\n            logger.error(\"メール送信に失敗しました\")\n            return False\n    \n    except Exception as e:\n        logger.error(f\"予期しないエラーが発生: {e}\", exc_info=True)\n        return False\n\nif __name__ == \"__main__\":\n    main()\n```\n\n### 5. requirements.txt（依存パッケージ）\n\n```\nrequests==2.31.0\nsendgrid==6.10.0\npython-dotenv==1.0.0\n```\n\n## 手順ステップバイステップ\n\n### ステップ1: 環境準備\n\nまずは新しいディレクトリを作成して、必要なパッケージをインストールします。\n\n```bash\nmkdir morning-news-system\ncd morning-news-system\npython -m venv venv\n\n# macOS/Linux\nsource venv/bin/activate\n\n# Windows\nvenv\\Scripts\\activate\n\npip install -r requirements.txt\n```\n\n### ステップ2: APIキーを取得\n\n1. **NewsAPI キー取得**\n   - https://newsapi.org に訪問\n   - メールアドレスで登録\n   - ダッシュボードからAPIキーをコピー\n\n2. **SendGrid キー取得**\n   - https://sendgrid.com で無料登録\n   - 「Settings」→「API Keys」でAPIキーを生成\n   - 送信元メールアドレスを確認（Verified Senders）\n\n### ステップ3: 環境変数ファイルを作成\n\nプロジェクトのルートに `.env` ファイルを作成します。\n\n```bash\ncat > .env << 'EOF'\nNEWS_API_KEY=your_news_api_key_here\nSENDGRID_API_KEY=your_sendgrid_api_key_here\nSENDER_EMAIL=your_sender_email@example.com\nRECIPIENT_EMAIL=your_recipient_email@example

---
※ ハッシュタグ: #AI #初心者
※ 価格設定: ¥0

---

## X導線ポスト（記事投稿後にURLを差し替えてXに投稿）

```
わたし最近知ったんですけど、Claude Codeで自分好みの朝刊システムが作れるんですって。毎朝勝手に届く…ってこういうことか。続きはnoteで詳しく解説してます
[noteリンク]
```

※ `[noteリンク]` を実際の記事URLに差し替えてください
